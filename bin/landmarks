#! /usr/bin/env python

import rospy
from randgen_omni_dataset import landmarks

LOG_LEVEL = rospy.DEBUG

if __name__ == '__main__':

    rospy.init_node('~', log_level=LOG_LEVEL)

    landmarks = landmarks.Landmarks(param='landmarks', topic='landmarks')
    rospy.spin()
